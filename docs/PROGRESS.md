# Progress log

Appended by `/phase-commit` at the end of each phase. Newest last.

| Phase | Date | Shipped | Deferred | Needs an ADR |
|---|---|---|---|---|
| GP5 spinner | 2026-08-26 | One reusable `<Spinner>` (accessible name via `aria-labelledby` on `role="status"`, `motion-reduce:animate-none`) swapped into every loading call site: `upload-button`, `generate-variants`, `chord-graph`, analyse's job-polling state (now surfacing the real `statusMessage`, previously computed but unrendered), and `listen-progressions`' unlabelled `animate-spin` div. All bespoke loading UI (bounce-bar animation, raw spinner div, text-swap buttons) deleted, not left alongside the new component. | `audio-recorder`'s "Recording..." status text (user-controlled state with a Stop button, not a passive wait — a spinner would misrepresent it); `chord-dropdown`'s silent no-indicator fetch (no existing loading UI there to swap, out of scope). | none |
| GP3 download page | 2026-09-03 | Code landed in `62d38c2` (2026-08-31); this row closes the phase. New `/result/[jobId]` route — thin async server wrapper + `ResultView` client component, `use-job-result` polling hook, `jobResult.ts` structural discriminators — rendering four states: running, succeeded (transcription / variants / progression shape), failed, and gone (unknown id or terminal job past `DATA_RETENTION_HOURS`, which the frontend can't and shouldn't distinguish). All three inline download call sites (`analyse/page.tsx`, `generate-variants/page.tsx`, `components/chord-graph.tsx` — the third found by grep, beyond the two the brief named) now link to the shared page; the `triggerBase64Download` helpers are deleted, not left alongside. Backend: `jobs.service.create_completed_job` persists synchronous variants/progression results straight through the existing store's create→ready→claim→complete sequence for a job-id link — no new store method, no migration; `GET /api/transcribe/{job_id}` additively reports a terminal job as `expired` once older than `DATA_RETENTION_HOURS`; additive `job_id` field on two responses. **Deployed verification (this session):** Vercel preview build of `62d38c2` serves `/result/[jobId]` at HTTP 200 with the correct SSR loading state (shared spinner, `motion-reduce:animate-none`) and degrades to the inline failed state — never a crash or blank page — on unreachable API, for both a real and a fabricated id; prod backend confirmed returning 202+`job_id` on create, `queued→processing→completed` with a full `TranscriptionResult` on poll (job `fe314b11…`), and HTTP 404 `{"detail":"Job not found"}` on an unknown id (the response `use-job-result` maps to the gone state). | Browser-rendered check of the succeeded (audio player + MIDI/WAV links) and gone empty states against the deployed app — blocked pre-merge because the prod backend's `CORS_ALLOWED_ORIGINS` allows only the production Vercel origin, so preview/localhost browser fetches are CORS-blocked. Both services auto-deploy from `main`; re-run the browser smoke test against `melody-generator-ai.vercel.app` immediately post-merge. All four states are covered at component level by `__tests__/pages/result.test.tsx`; `generate-variants` job creation by a mocked-model unit test (no weights locally). Backend pytest not re-run this session (deps not installed, zero backend changes since `62d38c2`; 106 passing at commit time, CI re-runs on the PR). Converting the synchronous variants/progression endpoints to real async jobs (no timeout problem to solve — out of scope). | none — the additive-contract / one-step-deploy decision and the "don't convert working sync endpoints to async" decision are both recorded in the `62d38c2` commit body; neither rises to an ADR |
| Prod OOM incident | 2026-09-04 | Not a guided-pass phase — ad-hoc production incident response, logged here since the fix commits reference this file. Root-caused recurring `/api/upload` 502/503s via Render's events API to `oomKilled` at the 512Mi limit (confirmed, not a crash/timeout/cold-start) — 14 OOM kills recorded since Aug 19. Four fixes landed in sequence, each verified against real production traffic: (1) `numpy<2` pin (`24e047f`) — `basic-pitch`'s `tflite-runtime` needs the NumPy 1.x ABI, an unpinned `numpy>=1.21.0` was resolving 2.2.6, so Basic Pitch was silently failing and falling back to heuristic notes on every request; (2) `.github/workflows/keep-warm.yml` (`cdadc8f`) — ping `/health` every 10 min so the free-tier instance doesn't idle-suspend; (3) lazy-import `torch` (`04459b0`) — `inference.py` and `colab_parity` were pulling in torch (~150-250MB) at module import even though only the generation routes need it, never the transcription path; idle RSS measured 503MB → 94MB post-fix, proven with `tests/test_lazy_imports.py` running a real transcription in a clean subprocess and asserting `torch` never lands in `sys.modules`; (4) `HEAVY_WORK_LOCK` + `_release_memory_to_os()` + `MAX_ANALYSIS_DURATION_SEC` 240s→60s (`bff5835`) — the transcribe worker and the generation routes had no shared lock and could run concurrently, each adding its own peak; and freed memory was never handed back to the OS, so one job's peak became every later job's floor (idle ratcheted 94MB→500MB+ over a handful of real transcriptions) — `tests/test_memory_management.py` proves the two pipelines' heavy-work sections no longer overlap. | GitHub Actions' `*/10` keep-warm schedule measured wildly unreliable in practice — 7 firings in 26 hours against ~150 expected, gaps up to 4h43m — so it's being demoted to a free secondary ping while the primary keep-warm mechanism moves to an external uptime monitor (set up outside this repo). No hard guarantee against OOM under real concurrent multi-user traffic: the measured fixed per-transcription memory floor (~350-450MB) already uses 65-85% of the 512Mi cap on its own. Honest ceiling stated to the user: workable for light, one-user-at-a-time demo traffic on the free tier; Render Standard (2GB, no idle-suspend) remains the answer if this needs to hold up under real concurrent load. | none — the "free-tier levers vs. paid upgrade" tradeoff was a conversational decision, not written up separately; worth a real ADR if roadmap Phase 13 (`/adr`) reaches infrastructure decisions |
| Chunked transcription (step 1) | 2026-09-06 | Step 1 of `docs/PLAN-full-transcription-split.md`. `_transcribe_and_mood_chunked` / `_transcribe_full_chunked` — transcribes the whole upload in overlapping 30s / 4s-overlap windows; `_merge_chunk_notes` welds notes across boundaries (welds only when the previous chunk truncated the note at its own edge, so re-articulations survive; chains ≥3 chunks; dedups full-overlap duplicates). Output MIDI full-length, `truncated` always `false`; default-clip analysis (first `MAX_ANALYSIS_DURATION_SEC`) reuses the merged notes + one librosa pass, no WAV render in chunked mode, falls back to single-pass when duration can't be probed (webm). New `JobStore.heartbeat` (protocol + SQLite & Postgres) called per chunk from the worker (`HEARTBEAT_LEASE_SECONDS` 180s) so a >900s job isn't reclaimed mid-run. `MAX_UPLOAD_DURATION_SEC` (600s) cap in the chunked path. Shipped behind `TRANSCRIBE_CHUNKED` (PR #14), flipped on in prod. **Measured**: synthetic 5-min clip → 788 notes / 12 chunks / full 300.0s, peak RSS **~400–430 MB flat across chunks** (memory is length-independent — the goal). | mir_eval chunked-vs-whole F1 validation on real music (follow-up harness). `_merge_chunk_notes` `edge_eps` 0.15s → ~0.5s (one boundary weld missed by 50ms on the synthetic clip). WAV preview for the full transcription (chunked mode returns `wav_b64: null`). | none |
| Free-tier hardening | 2026-09-07 | Decision: **stay on Render free (512MB) permanently** — the ~400–430 MB chunked-transcription peak fits, and paying $25/mo isn't warranted for a solo portfolio piece. Instead, closed the concurrent-request OOM risk in code: (1) `_run_generation` acquires `HEAVY_WORK_LOCK` **non-blocking** — a generation request that can't get it returns **429 + Retry-After** immediately with a clear message, instead of blocking (which piled up threadpool threads that all OOM'd when they finally ran); (2) `run_basic_pitch` waits `WORKER_HEAVY_WORK_WAIT_SEC` (180s) then raises → job re-queued (retryable), so the single worker thread never blocks forever; (3) `_load_cvae_iddm_locked` checks the `.pth` weights exist **before** importing torch — with no weights installed (current deployment) a generation request is a ~1ms 503 that never pays torch's ~150 MB, which on 512 MB is the difference between fitting and OOM-ing a later transcription in the same process. `CLAUDE.md` + `README.md` + the `architecture-reference` skill now state the single-concurrent-task constraint as a deliberate, documented limitation — not something hidden. New tests: 429-when-locked, worker-reraise-when-locked, 503-without-torch-when-weightless. | Phase B (separate): measure `TRANSCRIBE_CHUNK_SEC` 30→15 for a lower peak; audit heavy imports (`matplotlib`/`music21`/`resampy`) for what's actually loaded at runtime; trim the Docker image. `healthCheckPath` still unset on the Render service (dashboard fix). | worth an ADR if roadmap Phase 13 reaches infra decisions — free-tier-vs-paid was a conversational call, now settled |
| Free-tier hardening — Phase B (import audit) | 2026-09-07 | Ran the import-surface audit the row above deferred, in a fresh interpreter with `/proc/self/status` VmRSS deltas (throwaway CI diagnostic — backend deps don't install locally). **`music21` was the only removable heavy import: +35 MiB RSS at every boot** (239 submodules) for key detection that `_key_from_histogram` — a full Krumhansl-Schmuckler implementation, already the fallback — does for free. Removed: the import, the `_extract_key_label` music21 wrapper (call sites now call `_key_from_histogram` directly), `music21_available` from `/health`, `music21` from `requirements.txt`. `torch` confirmed **never imported** on the transcription path (lazy-torch fix measured, not assumed); `matplotlib` / `scikit-learn` / `tensorflow` **never imported at all** — the "sklearn in the floor" line in earlier docs was wrong, corrected here and in the skill + memory. `librosa` / `scipy` / `numba` / `tflite_runtime` / `mir_eval` / `resampy` are all pulled eagerly by `basic_pitch.inference` + warm-up — deferring them would only move cost into the transcription peak, not remove it. **`TRANSCRIBE_CHUNK_SEC` 30→15: measured on a 90s clip, no peak benefit** — per-chunk Basic Pitch working set is ~11 MiB either way (fixed model-activation + decode-buffer cost dominates); halving the window just doubles the `predict()` count and the boundary-weld surface. Kept at 30. **No per-transcription ratchet**: 4 sequential single-pass transcriptions flat at 271 MiB (`malloc_trim` works in the deployed glibc). New permanent test: `test_transcription_path_does_not_import_music21_or_other_dead_weight`. | `torch==2.13.0` is ~200 MB of the Docker image and generation 503s before importing it (no `.pth` weights ship) — but that's disk, not the RAM ceiling, and removing it declares generation permanently off in this deployment → left as a user decision. `healthCheckPath=/health` still unset on the Render service (dashboard-only). **Prod idle floor after the music21 deploy: ~309 MB** (was ~326), measured on the deployed instance — a ~15–17 MB reduction (less than the 35 MiB isolated CI import cost; glibc arena / cgroup accounting differ, and part of music21's pages overlap deps loaded anyway). | none |
| ffmpeg in the Docker image | 2026-09-08 | Production incident. **Every transcription of a browser recording was failing** — Render logs: `LibsndfileError: Format not recognised` → `FileNotFoundError: 'ffmpeg'`. The frontend records `audio/webm;codecs=opus`; libsndfile can't decode webm so `_read_audio_bytes` shells out to `ffmpeg` — but the Dockerfile only installed `fluidsynth`, never `ffmpeg`, so the fallback had **never** worked in the deployed container (only uploaded `.wav`/`.flac`/`.ogg` transcribed). Pre-existing, surfaced by recording instead of uploading a WAV. Fix: `apt-get install ... fluidsynth ffmpeg`; `_read_audio_bytes` catches `FileNotFoundError` → clean `HTTPException(400)` instead of "failed unexpectedly". PR #19. **Verified in prod**: an AAC file (libsndfile can't read → forces the ffmpeg path) transcribed `processing 50 → completed 100`. | Frontend could encode WAV client-side to drop the server ffmpeg dependency for recordings — not now. | none |
| `/api/analyze` — MIDI-based (step 2) | 2026-09-08 | Step 2 of `docs/PLAN-full-transcription-split.md`. `POST /api/analyze {job_id, clip_start_sec?, clip_end_sec?}` → mood / key / tempo / chords / pitch histogram for that clip window, computed from the transcribe job's persisted `note_events`. **No librosa, no Basic Pitch, no audio decode, no `HEAVY_WORK_LOCK`** — re-runnable against any window for ~free. `analyze_clip()` (the computation on a note-event slice; empty window → neutral defaults), `_chords_from_note_events()` (windowed duration-weighted pitch-class vector → the same `_infer_chord_from_chroma` template match) replaces `_detect_chords_from_audio` in `_transcribe_and_mood` + `_transcribe_and_mood_chunked` (chroma version kept for `generate_from_audio`); the chunked path now only decodes `clip_audio` when there are zero notes (one fewer librosa pass, ~15–20 MB less transient). `_trim_note_events()` persists a compact `{start,end,pitch,velocity}` form in the transcribe result. `/generate-variants` gains `clip_start_sec`/`clip_end_sec`. `MAX_UPLOAD_DURATION_SEC` best-effort at `/api/upload` (header probe). **Contract step 1 (additive)**: `/api/transcribe` result *gains* `note_events`, keeps everything. TS: `NoteEvent` + optional `note_events?` on `TranscriptionResult`, new `analyzeClip.ts`. PR #20, 145 backend tests pass. | `/api/analyze` has no rate limit yet (cheap, but a public unauthenticated DB read). | none |
| Two-result `/analyse` (step 3) | 2026-09-08 | Step 3 of `docs/PLAN-full-transcription-split.md`. `/analyse` splits into an independent **transcription** result (full MIDI + downloads, note count, source duration — no analysis fields) and a **clip analysis** result (`POST /api/analyze` for a user-chosen `[start, end]`, auto-run on the first 60 s, re-runnable without re-transcribing). New `components/clip-range.tsx`: two native range sliders (no waveform — roadmap phase 7), keyboard-operable, `aria-valuetext` mm:ss, `aria-live` region for the pending window, submit fires a request only on an actual change. `page.tsx`: separate request-id + `AbortController` guards for transcription vs analysis — a slow/failed analysis never touches the transcription card, a superseded analysis is aborted. `result-view.tsx` (`/result/[jobId]`) stops reading `key`/`mood_label`/`tempo_bpm` (removed in step 5) — shows notes + duration. PR #22. **Verified on the deployed app** (playwright): upload → transcription card ("18 notes · 0:08") + clip analysis card (Mood happy, Key C major, Tempo, histogram, chords "C"); drag start slider → live region announces "Pending analysis window 0:03–0:08" → "Analyse this section" → re-fetch, "Notes in window" drops 18→12. Network: `upload 201`, `transcribe 202`, 5× poll `200`, 2× `analyze 200`, no console errors. | `torch==2.13.0` in the Docker image still dead weight. mir_eval chunked-vs-whole F1 validation harness + `_merge_chunk_notes` `edge_eps` tuning. | none |
| Finalize the analyze split (steps 4 + 5) | 2026-09-09 | Steps 4 + 5 of `docs/PLAN-full-transcription-split.md`, one PR. **Step 4:** removed the `TRANSCRIBE_CHUNKED` env flag + the `if/else` in `run_basic_pitch` — `_transcribe_and_mood_chunked` is the only path (flag was already `true` in prod since step 1, so no behaviour change; verified via a fresh prod transcribe showing `n_chunks`). `_transcribe_and_mood` stays as the un-probeable-audio fallback (browser webm/opus) + the generation-path transcription. No WAV preview for the full-length MIDI. **Step 5:** `/api/transcribe`'s result dropped `mood_label` / `mood_idx` / `detected_chords` / `key` / `pitch_histogram` / `tempo_bpm` / `average_pitch` — keeps `n_notes` / durations / `truncated` / `midi_*` / `wav_*` (null/empty) / `n_chunks` / `note_events`. `_transcribe_and_mood_chunked` stopped computing them too. TS `TranscriptionResult`: 7 fields removed, `note_events` required, `n_chunks?` added. `result-view.tsx` transcription branch → MIDI download only. **Contract step 3, backward-compatible in either deploy order** — the frontend stopped reading these in step 3, so present-but-unread and absent-but-not-read are both no-ops; `tsc` confirms no non-test code touched them. Docs (README / CLAUDE.md / architecture-reference / PLAN) updated. | WAV preview for the analyse clip (would need bounded fluidsynth render — deferred). `torch` still in the image. mir_eval F1 harness + `edge_eps`. Canvas waveform → roadmap phase 7. `TRANSCRIBE_CHUNKED` env var can be removed from the Render service (now a no-op). | none — the "additive then remove, either deploy order safe" contract call is recorded in the commit body |
| Model weights delivery + analyse fixes | 2026-09-10 | **Weights:** the trained joint checkpoint (`joint_e2e_weights.pth`, ~1.4 MB) is **not committed** (user's call — repo is public, and committing it would activate torch on the 512 MB tier permanently). Instead `_ensure_joint_weights()` (stdlib `urllib`, no torch, single-flighted under the load lock) fetches it on the first generation request from `MODEL_WEIGHTS_URL` (a GitHub release asset) and caches it on the ephemeral disk. Unset → 503 as before; download-failed with a URL set → **503 *without* caching** so the next request retries. Local copy already in place for dev. New `test_model_weights_fetch.py` (8 tests). **`/analyse` "analysis gone" — two root causes, both env not code:** (1) the local **backend** had been running since Sunday with **no `--reload`**, frozen on pre-PR-#18 code with no `/api/analyze` route → frontend 404s; restarted it. (2) the local **frontend** dev server was a 2–3-day-old process with a route manifest corrupted by the earlier merge-conflict-marker churn, serving `/analyse` as not-found; killed it + `rm -rf .next` + fresh start. Deployed `/analyse` was always fine (re-verified end-to-end). **Chord display (PR #27):** progression now shown in playback order (`C → Am → F → G`) with a "Most used" summary instead of grouped-by-root; blank-card fallback button. | `gh release create` for the weights asset + setting `MODEL_WEIGHTS_URL` on Render are manual (blocked from here). Prod generation memory not yet measured with weights active. Old pre-session `feat/*` remote branches still on GitHub. | worth an ADR if the free-tier generation memory turns out to need a real decision |
| Prod generation memory + client-side playback | 2026-09-11 | **Measured** (`MODEL_WEIGHTS_URL` set on Render): idle floor **~313 MB** (torch not loaded) → first generation **~455 MB peak** (30.3s cold: weights download + torch import + model load; 7.4s warm after) → holds flat at ~455 MB. **57 MB headroom under the 512 MB cap, no OOM, no restart** — generation does not need a rollback. Surfaced two follow-ups from that run: no audible output (FluidSynth's Python binding isn't installed in prod, so variants fell back to a sine WAV or nothing) and a "poisoned floor" (torch's ~150–170 MB is permanent per-container once a generation runs). **Poisoned floor — measured, not fixed in code**: a local probe (real joint checkpoint, 4 variant forward passes) showed `gc`+`malloc_trim` recover 0 MB (196→196 MB) and purging `torch` from `sys.modules` also recovers 0 MB **and segfaults on the next `import torch`** (global dispatcher/thread-pool state can't reinit) — matches prod holding at ~455 MB for 5+ min after a generation. Documented in CLAUDE.md as a known limitation (stale-job reclaim + `max_attempts=3` already degrade the failure mode to "one slow retry", not data loss); the real fix (subprocess isolation) is deferred until generation traffic is real. **Playback, shipped instead of paying for FluidSynth**: melodies are now synthesized client-side — `app/lib/midi.ts` (pure MIDI parse via `@tonejs/midi`), `hooks/use-midi-player.ts` (Tone.js lifecycle behind a dynamic `import()`, discriminated-union state, disposes on unmount), `components/midi-player.tsx` (accessible Play/Pause, progressbar, reduced-motion-safe). Wired into both variant surfaces, replacing the `wav_b64 ? <audio> : "unavailable"` block. Piano samples (Salamander Grand Piano subset, CC-BY 3.0, 9 files / 664 KB) are committed under `frontend/public/audio/piano/` rather than CDN-fetched, so playback has no third-party runtime dependency — reconciled against CLAUDE.md's no-committed-audio rule in `docs/BASELINE.md` (same precedent as the tracked `.sf2`/`background.mp4`). Backend: `generate_iddm_variants` no longer calls `_midi_bytes_to_wav_b64` (extracted `_variant_entry()`, pure/unit-tested); `wav_b64`/`wav_filename` stay in the response shape as always-null/empty — **no contract change, either deploy order**. Verified: `tsc`/`prettier`/`npm test` clean (18 suites, 95 tests, 3 new), backend 149 pass, `npm run build` confirms `tone` is a lazy chunk (not in any entry bundle) and `/generate-variants` still prerenders static, and a real local generation exercised through Playwright (Play → Pause toggle, samples + tone chunks load 200, zero console errors). | Subprocess isolation for the poisoned floor (only worth it once generation traffic is real). `pyfluidsynth` was considered and rejected in favor of client-side playback (keeps the memory win). WAV preview for the analyse clip and the progression result still render server-side (unaffected, out of scope). Old pre-session `feat/*` remote branches still on GitHub. | none — the "don't re-attempt in-process torch unloading" and "commit a small audio asset on purpose" calls are both written down in CLAUDE.md / BASELINE.md respectively |
| Post-deploy e2e verification + cold-start root cause | 2026-09-15/16 | **E2E against prod** (Render `917e29f` + Vercel, both live): real upload→transcribe, `/api/analyze`, a real microphone recording through the webm/opus→ffmpeg fallback path, and a real `generate-variants` call all verified working end-to-end via curl + Playwright against the user's live browser — playback position visibly advanced during a real `Play melody` click (0:00→0:07), zero console errors, and the `FluidSynth render failed` log line is now absent from every generation (confirms the prior phase's server-side-WAV removal is live). **Idle RSS unchanged** post-deploy (~316–317 MB, statistically the same as the prior ~313 MB baseline — expected, WAV removal only touched the generation path). **New finding**: a cold-start generation spiked to **~521.7 MB (~497.5 MiB) — only ~15 MB under the 512 MiB cap** (vs a consistent ~455 MB warm), one real prod sample; survived without OOM. Root-caused locally (real `app.inference` functions, staged RSS marks, isolated subprocesses) rather than guessed: `torch`'s own marginal import cost is only **~5 MB** once basic-pitch/numpy/librosa are already resident from boot warm-up (they share the same BLAS/OpenMP libraries) — contrary to the earlier framing that blamed `torch` import itself. The real cost is two **first-call JIT/backend-compile** hits boot warm-up never pays today: `_read_audio_bytes`'s librosa resample path (**+66.7 MB** on the first real call) and `_transcribe_and_mood`'s Basic Pitch predict on real, non-silent audio (**+48.8 MB** on the first real call) — both drop to a few MB on a second call. Quantified two independent fixes as isolated-subprocess `ru_maxrss` peaks: skipping the redundant internal Basic Pitch call (**-22.9 MB**) and warming boot with a real audio clip through the real decode path (**-43.2 MB**, not implemented this phase — flagged, not applied without being asked); combined **-57.5 MB**. **Shipped this phase**: `/api/generate-variants` accepts an optional `job_id` — when it points at a completed transcribe job, `generate_iddm_variants` skips re-running Basic Pitch entirely and builds the seed MIDI + mood straight from the job's stored `note_events` (`_slice_note_events` extracted from `analyze_clip` and shared; new `_transcription_summary_from_note_events`). Additive/backward-compatible (no `job_id` → unchanged `file`/`filename`+`upload_id` path). Frontend: `UploadSession.jobId` threads through from `/analyse`'s transcribe call to `/generate-variants`'s "use my last upload". Model weights are now prefetched at boot (`warm_up_model_weights`, unconditional — unlike Basic Pitch's warm-up, generation always runs in the web process regardless of `RUN_WORKER_IN_PROCESS`), removing the download from the first generation request's critical path (file is ~1.4 MB; not a memory lever, a latency/robustness one). **Measured locally, real weights + a real transcribe job**: `generate-variants` with `job_id` — 141 ms, mood matched what `/api/analyze` already reported for the same job; the same audio via `file=` (re-transcribing) — 756 ms. **~5.4× faster**, and the redundant Basic Pitch pass is confirmed skipped. | The bigger lever (real-clip boot warm-up, -43.2 MB alone) — deferred, not requested this phase; worth a fast follow-up if the next item still doesn't close the margin. **Redeploy-based cold-start re-measurement (2-3 forced-fresh instances) — blocked on user-granted `trigger_deploy` access**, explicitly deferred to right after this lands; that's the real test of whether the ~15 MB margin actually widens. Old pre-session `feat/*` remote branches still on GitHub. | none — root cause and both fix decisions (what to apply now vs. defer) are written down here and in the approved plan; the redeploy-measurement follow-up is a to-do, not a design question |
